name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    name: Backend python
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Run tests
        run: python -m pytest backend/tests
  # --- Frontend (React) ---
  # Avkommentera när ni har en fungerande frontend med package.json + npm test
  #
  # frontend:
  #   name: Frontend (React)
  #   runs-on: ubuntu-latest
  #
  #   defaults:
  #     run:
  #       working-directory: frontend
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #         cache: "npm"
  #         cache-dependency-path: frontend/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Run tests
  #       run: npm test